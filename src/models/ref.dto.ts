/* Classe que serve como referencia ao objeto Cliente, EnderecoEntrega e Pedido, identificados por id dentro do objeto 
Pagamento no FrontEnd. Ver pdf Aula 3 */
export interface RefDTO{
    id: string;
}